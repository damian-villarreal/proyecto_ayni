
@{
    ViewBag.Title = "pv_EliminarModal";
}

<!-- Main navigation -->
<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Ingreso al sistema</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body text-center" ">
                <form class="form-signin" action="@Url.Action("Iniciar", "Sesion")" method="post">
                    <h1 class="h3 mb-3 font-weight-normal">Ingrese sus credenciales</h1>
                    <div class="col">
                        <!-- NombreUsuario -->
                        <div class="md-form">
                            <label for="NombreUsuario">Para confirmar esta acción debe ingresar su contraseña: </label>
                        </div>
                    </div>
                    <div class="col">
                        <!-- Contraseña -->
                        <div class="md-form">
                            <label for="Password">Contraseña</label>
                            @Html.TextBox("Password", "", new { @class = "form-control", @type = "password" })
                        </div>
                    </div>
                    <input class="btn btn-lg btn-primary btn-block" type="submit" name="enviarAutenticacion" value="Ingresar" />
                </form>
            </div>
        </div>
    </div>
</div>

